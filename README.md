# Yex FFI Module Template

# Building
 `cargo build`

# How to use?
 You need to install yex from master branch.
 `cargo install --git https://github.com/nonamescm/yex-lang.git`
 
 And run the file with yex!
 `yex main.yex`
